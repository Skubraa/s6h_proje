

{% form_theme form 'form/tailwind_layout.html.twig' %}

{% block title %}{% endblock %}

{# 2. Ana Kart Yapısı #}
<div class="bg-white dark:bg-gray-800 shadow-2xl rounded-3xl overflow-hidden border border-gray-100 dark:border-gray-700 transition-all duration-300">
    
    {# Kart Üst Kısmı (Add dosyasındaki gibi gri arka plan ve başlık düzeni) #}
    <div class="p-8 border-b border-gray-100 dark:border-gray-700 bg-gray-50/50 dark:bg-gray-800/50">
        <h1 class="text-3xl font-extrabold text-gray-900 dark:text-white tracking-tight">
            <span class="text-blue-600 dark:text-blue-400">Profil Ayarları</span>
        </h1>
        <p class="mt-2 text-gray-500 dark:text-gray-400 text-sm">
            Bilgilerini güncelleyerek seni daha iyi tanımamızı sağla.
        </p>
    </div>

    {# 3. Form Alanı #}
    <div class="p-8">
        {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'space-y-8'}}) }}
            
            {# 1. TEMEL BİLGİLER #}
            <section>
                <div class="flex items-center gap-2 mb-4 text-indigo-600 dark:text-indigo-400 font-bold border-b border-gray-100 dark:border-gray-700 pb-2">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                    Temel Bilgiler
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    {{ form_row(form.name, {'label': 'Ad Soyad'}) }}
                    {{ form_row(form.dateOfBirth, {'label': 'Doğum Tarihi'}) }}
                </div>
                <div class="mt-4">
                    {{ form_row(form.bio, {'label': 'Biyografi', 'attr': {'placeholder': 'Kendinden bahset...'}}) }}
                </div>
            </section>

            {# 2. İLETİŞİM VE SOSYAL MEDYA #}
            <section>
                <div class="flex items-center gap-2 mb-4 text-indigo-600 dark:text-indigo-400 font-bold border-b border-gray-100 dark:border-gray-700 pb-2">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.826a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path></svg>
                    Bağlantılar
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    {{ form_row(form.websiteUrl, {'label': 'Web Sitesi'}) }}
                    {{ form_row(form.xUsername, {'label': 'X (Twitter) Kullanıcı Adı'}) }}
                </div>
            </section>

            {# 3. İŞ VE KONUM #}
            <section>
                <div class="flex items-center gap-2 mb-4 text-indigo-600 dark:text-indigo-400 font-bold border-b border-gray-100 dark:border-gray-700 pb-2">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                    Kariyer ve Konum
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    {{ form_row(form.company, {'label': 'Şirket / Okul'}) }}
                    {{ form_row(form.location, {'label': 'Konum'}) }}
                </div>
            </section>

            <div class="hidden">
                {{ form_row(form.user) }}
            </div>

            <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-4 px-6 rounded-2xl shadow-lg shadow-indigo-500/30 transition duration-200 transform active:scale-95 flex items-center justify-center gap-2 text-lg">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                Profilimi Kaydet
            </button>

        {{ form_end(form) }}
    </div>

    {# Kart Alt Kısmı (Vazgeç Butonu - Add dosyasındaki gibi eklendi) #}
    <div class="px-8 py-6 bg-gray-50 dark:bg-gray-800/80 border-t border-gray-100 dark:border-gray-700 flex justify-center">
        <a href="{{ path('app_profile', {id: app.user.id}) }}" class="text-sm font-medium text-gray-500 hover:text-red-500 dark:text-gray-400 dark:hover:text-red-400 transition-colors flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            Vazgeç ve Profile Dön
        </a>
    </div>
</div>
